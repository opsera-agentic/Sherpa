# Fail `npm install` fast on unsupported Node versions instead of warning.
# better-sqlite3 is a native module: installing under one Node version and
# running under another causes ERR_DLOPEN_FAILED (NODE_MODULE_VERSION mismatch).
engine-strict=true
